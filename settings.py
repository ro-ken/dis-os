

# 本机架构：win , mac , smp , hwj , ywd

arch = "win"        # 本机架构


# 环境：”dev“本机开发，”exp“，联机实验，”local_exp“,本地实验
env = "local_exp"

show_result = False  # 是否输出结果

# 调度类型："static" 根据静态表调度，"dynamic"根据实时获取资源进行动态调度
sched_type = "static"

heart_rate = 2  # 设置心跳频率

show_server_heart_res = True  # 实时显示心跳结果

show_client_heart_res = True  # 实时显示心跳结果